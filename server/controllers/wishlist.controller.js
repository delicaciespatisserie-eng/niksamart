const Wishlist = require('../models/Wishlist');
exports.getWishlist = async (req, res) => { try { const wishlist = await Wishlist.findOne({ customerId: req.user._id }).populate('products'); res.json({ success: true, wishlist: wishlist || { products: [] } }); } catch (error) { res.status(500).json({ success: false, message: error.message }); } };
exports.toggleWishlist = async (req, res) => { try { const wishlist = await Wishlist.findOneAndUpdate({ customerId: req.user._id }, { $setOnInsert: { customerId: req.user._id } }, { upsert: true, new: true }); const id = req.params.productId; const exists = wishlist.products.some(p => String(p) === String(id)); wishlist.products = exists ? wishlist.products.filter(p => String(p) !== String(id)) : [...wishlist.products, id]; await wishlist.save(); res.json({ success: true, wishlist, added: !exists }); } catch (error) { res.status(500).json({ success: false, message: error.message }); } };
