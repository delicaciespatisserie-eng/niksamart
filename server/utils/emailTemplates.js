const shell = (title, body) => `<div style="font-family:Inter,Arial,sans-serif;background:#f7f7fb;padding:24px"><div style="max-width:640px;margin:auto;background:#fff;border-radius:12px;overflow:hidden"><div style="background:#000080;color:#fff;padding:22px"><h1 style="font-family:Georgia,serif;color:#C8A84B;margin:0">${title}</h1></div><div style="padding:24px;color:#1f2937;line-height:1.6">${body}</div><div style="padding:18px 24px;background:#000080;color:#fff;font-size:12px">Niksa Global Ventures Pvt. Ltd. | niksamart.com</div></div></div>`;
const welcomeEmail = (name) => shell('Welcome to Niksa Mart', `<p>Hello ${name},</p><p>Your account is ready. Discover Niksa Bilona Gold and pure marketplace products from trusted vendors.</p>`);
const orderConfirmation = (order, customer) => shell('Order Confirmed', `<p>Hello ${customer.name},</p><p>Your order <strong style="color:#C8A84B">${order.orderNumber}</strong> is confirmed. Total: ₹${order.totalAmount}.</p>`);
const vendorNewOrder = (order, vendor) => shell('New Vendor Order', `<p>Hello ${vendor.shopName},</p><p>You have items in order <strong>${order.orderNumber}</strong>. Please process them from your dashboard.</p>`);
const orderDelivered = (order) => shell('Order Delivered', `<p>Your order <strong>${order.orderNumber}</strong> has been delivered.</p><p><a style="color:#000080" href="${process.env.CLIENT_URL}/account/reviews">Share your review</a> and help other customers choose well.</p>`);
const resetPassword = (resetUrl) => shell('Reset Your Password', `<p>Click the button below to reset your Niksa Mart password.</p><p><a href="${resetUrl}" style="display:inline-block;background:#C8A84B;color:#000080;padding:12px 18px;border-radius:8px;text-decoration:none;font-weight:700">Reset password</a></p>`);
module.exports = { welcomeEmail, orderConfirmation, vendorNewOrder, orderDelivered, resetPassword, resetPasswordEmail: resetPassword };
