import { useAdminVendorsQuery } from '../../store/api/vendorApi';
export default function AdminVendors(){const{data,isLoading}=useAdminVendorsQuery();const vendors=data?.vendors||[];return <main className="mx-auto max-w-7xl px-4 py-10"><h1 className="font-heading text-5xl text-navy">Vendor Queue</h1><div className="card mt-6 overflow-x-auto p-5">{isLoading?<p>Loading...</p>:<table className="w-full text-left"><thead><tr><th>Shop</th><th>Owner</th><th>Status</th><th>Action</th></tr></thead><tbody>{vendors.map(v=><tr className="border-t" key={v._id}><td>{v.shopName}</td><td>{v.userId?.email}</td><td>{v.isApproved?'Approved':'Pending'}</td><td><button className="btn-gold">Approve</button></td></tr>)}</tbody></table>}</div></main>}
