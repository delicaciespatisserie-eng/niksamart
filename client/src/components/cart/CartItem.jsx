import { FaTrash } from 'react-icons/fa';
import { money } from '../../utils/helpers';
export default function CartItem({ item, onQty, onRemove }) { const product = item.productId || {}; return <div className="grid gap-4 border-b py-4 sm:grid-cols-[80px_1fr_120px_120px_40px] sm:items-center"><img src={product.thumbnail || '/logo.png'} alt={product.name} className="h-20 w-20 rounded object-contain bg-gray-50" /><div><h3 className="font-semibold text-navy">{product.name}</h3><p className="text-sm text-gray-500">{item.vendorId?.shopName || 'Niksa Vendor'}</p></div><input className="input" type="number" min="1" value={item.qty} onChange={e => onQty(item.productId._id, e.target.value)} /><b>{money((item.price || product.salePrice) * item.qty)}</b><button className="text-red-600" onClick={() => onRemove(item.productId._id)}><FaTrash /></button></div>; }
