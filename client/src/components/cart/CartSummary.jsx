import { money } from '../../utils/helpers';
export default function CartSummary({ subtotal = 0 }) { const tax = Math.round(subtotal * .05); const delivery = subtotal > 999 || subtotal === 0 ? 0 : 60; return <div className="card sticky top-24 p-5"><h2 className="font-heading text-2xl text-navy">Order Summary</h2><input className="input mt-4" placeholder="Coupon code" /><div className="mt-5 space-y-3 text-sm"><p className="flex justify-between"><span>Subtotal</span><b>{money(subtotal)}</b></p><p className="flex justify-between"><span>GST 5%</span><b>{money(tax)}</b></p><p className="flex justify-between"><span>Delivery</span><b>{money(delivery)}</b></p><p className="flex justify-between border-t pt-3 text-lg"><span>Total</span><b className="text-gold">{money(subtotal + tax + delivery)}</b></p></div><a href="/checkout" className="mt-5 block text-center btn-gold">Checkout</a></div>; }
