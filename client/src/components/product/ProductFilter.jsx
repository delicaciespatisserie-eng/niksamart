import { categories } from '../../utils/constants';
export default function ProductFilter({ params, setParam }) { return <aside className="space-y-6 rounded-lg border p-4"><div><h3 className="font-bold text-navy">Categories</h3>{categories.map(c => <label key={c} className="mt-3 flex gap-2 text-sm"><input type="checkbox" checked={params.get('category') === c} onChange={() => setParam('category', params.get('category') === c ? '' : c)} />{c}</label>)}</div><div><h3 className="font-bold text-navy">Price Range</h3><input className="input mt-3" placeholder="Min" value={params.get('minPrice') || ''} onChange={e => setParam('minPrice', e.target.value)} /><input className="input mt-3" placeholder="Max" value={params.get('maxPrice') || ''} onChange={e => setParam('maxPrice', e.target.value)} /></div><label className="flex gap-2 text-sm"><input type="checkbox" onChange={e => setParam('stock', e.target.checked ? '1' : '')} /> In stock</label></aside>; }
